Purpose

This file captures repository-specific guidance for Copilot/assistant sessions so future automated or human-driven sessions start with correct context.

Summary of current repository state (auto-detected)

- Files detected: .gitignore
- No README.md, package.json, pyproject.toml, setup.py, Makefile, or other build/test config files detected.

Build, test, and lint commands

- No explicit build/test/lint commands were found in the repo. Please update this section when adding build tooling.

Examples to replace with real commands (delete examples once real commands are present):
- Node.js
  - Build: npm run build
  - Test (full): npm test
  - Single test: npx jest path/to/file.test.js -t "test name"  OR npm test -- -t "test name"
  - Lint: npm run lint

- Python
  - Test (full): pytest
  - Single test: pytest tests/test_module.py::test_name
  - Lint: flake8 src/ or pylama

- .NET
  - Build: dotnet build
  - Test (single): dotnet test --filter FullyQualifiedName~Namespace.Class.TestName

- Java (Maven)
  - Test (single): mvn -Dtest=ClassName#testMethod test

High-level architecture (big picture)

- Repository appears to be empty of source code at the time of generation. No components, entrypoints, or services were detectable.
- When code is added, update here with a short (2–4 line) overview of the main components and boundaries. Keep it focused on runtime structure (services, libraries, CLI, web server) and how code is organized across top-level directories.

Key conventions (repo-specific)

- No project-specific conventions detected. Add conventions that matter here, for example:
  - Test file naming (tests/ vs. __tests__/, suffix _test, etc.)
  - Preferred test runner and command to run a single test
  - Naming patterns for public APIs or modules
  - Any required commit trailers or CI checks

Integrations and AI assistant configs

- No additional AI assistant files found (CLAUDE.md, .cursorrules, AGENTS.md, .windsurfrules, CONVENTIONS.md, etc.). If the project uses any, copy relevant sections here so Copilot sessions pick them up.

How to update this file

- Edit .github/copilot-instructions.md to include detected build/test/lint commands and a short architecture summary after adding source files.
- Keep commands exact (copy from package.json scripts, Makefile targets, tox.ini, or similar).

Maintainers / Contacts

- Not detected. Add owner names or team contacts if helpful for automated assistants.

Last auto-scan

- 2026-06-04T19:22:55+03:00 (generated by Copilot CLI)

